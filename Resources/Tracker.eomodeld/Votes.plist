{
    attributes = (
        {
            columnName = "bug_id"; 
            externalType = mediumint; 
            name = bugId; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "vote_count"; 
            externalType = smallint; 
            name = voteCount; 
            precision = 5; 
            valueClassName = NSNumber; 
            valueType = s; 
        }, 
        {
            columnName = who; 
            externalType = mediumint; 
            name = who; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (bugId, voteCount, who); 
    className = EOGenericRecord; 
    classProperties = (bugId, voteCount, who); 
    externalName = votes; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {"_clientClassPropertyNames" = (bugId, voteCount, who); uniqueID = 196630496; }; 
    name = Votes; 
    primaryKeyAttributes = (bugId); 
    userInfo = {fetchSpecificationDictionary = {}; }; 
}

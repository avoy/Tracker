{
    attributes = (
        {
            allowsNull = Y; 
            columnName = added; 
            externalType = tinytext; 
            name = added; 
            valueClassName = NSString; 
            valueType = S; 
            width = 255; 
        }, 
        {
            columnName = "attach_id"; 
            externalType = mediumint; 
            name = attachId; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "bug_id"; 
            externalType = mediumint; 
            name = bugId; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = "bug_when"; 
            externalType = datetime; 
            name = bugWhen; 
            valueClassName = NSCalendarDate; 
            valueType = T; 
        }, 
        {
            columnName = fieldid; 
            externalType = mediumint; 
            name = fieldid; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            allowsNull = Y; 
            columnName = removed; 
            externalType = tinytext; 
            name = removed; 
            valueClassName = NSString; 
            valueType = S; 
            width = 255; 
        }, 
        {
            columnName = who; 
            externalType = mediumint; 
            name = who; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (fieldid, bugWhen); 
    className = EOGenericRecord; 
    classProperties = (attachId, fieldid, who, removed, bugWhen, bugId, added); 
    externalName = "bugs_activity"; 
    internalInfo = {
        "_clientClassPropertyNames" = (attachId, fieldid, who, removed, bugWhen, bugId, added); 
        uniqueID = 196630727; 
    }; 
    name = BugsActivity; 
    primaryKeyAttributes = (bugWhen, fieldid); 
    userInfo = {fetchSpecificationDictionary = {}; }; 
}

{
    attributes = (
        {
            columnName = "bug_id"; 
            externalType = mediumint; 
            name = bugId; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }, 
        {
            columnName = who; 
            externalType = mediumint; 
            name = who; 
            precision = 7; 
            valueClassName = NSNumber; 
            valueType = i; 
        }
    ); 
    attributesUsedForLocking = (bugId, who); 
    className = EOGenericRecord; 
    classProperties = (who); 
    externalName = cc; 
    fetchSpecificationDictionary = {}; 
    internalInfo = {"_clientClassPropertyNames" = (who); uniqueID = 196630471; }; 
    name = Cc; 
    primaryKeyAttributes = (bugId); 
    userInfo = {fetchSpecificationDictionary = {}; }; 
}
